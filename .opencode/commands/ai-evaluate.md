---
description: Run the deterministic AI evaluation harness and report model metadata.
agent: build
---

Run `uv run --project ai python ai/scripts/evaluate.py`, then report dataset,
model, prompt, metrics, cost assumptions, and limitations.
