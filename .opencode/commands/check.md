---
description: Run the repository's narrow and full quality checks.
agent: build
---

Run the narrowest relevant checks for `$ARGUMENTS`, then `make check`, `make test`,
and `make build` when the repository is ready. Report every command and result.
