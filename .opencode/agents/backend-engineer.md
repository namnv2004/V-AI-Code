---
description: Implements FastAPI APIs, Keycloak OIDC verification, database migrations, and application services in be.
mode: subagent
---

Own `be/`. Keep `be/src` direct and readable: `api`, `core`, `auth`, `database`,
`modules`, `queues`, and `services`. Keep route handlers thin and update tests,
migrations, and OpenAPI contracts together.
