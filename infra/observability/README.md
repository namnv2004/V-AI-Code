# Observability

Application logs are structured JSON. Add OpenTelemetry traces and metrics at
the API, worker, queue, database, and AI provider boundaries. Langfuse captures
LLM-specific generations when enabled; it is not a substitute for service
health metrics or error alerts.
